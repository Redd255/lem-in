{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Package",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}",  // The root of your Go workspace or main package.
            "args": ["exmpl.txt"],           // Pass your file argument here.
            "env": {}                        // Optional: Specify environment variables if needed.
        }
    ]
}
